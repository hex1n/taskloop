# Review 0c3702f8-81a4-4464-ae06-65b28e0d2ea3

- task: a15d791d-7511-43c3-a50f-a457b38a7e1f (install.mjs closes the taskloop-to-workloop upgrade gaps and the open-atomicity defect that locked this repo)
- level: second_model
- reviewer: claude-opus-4-8 (general-purpose subagent, distinct model from the authoring session's fable-5; ran the installer suite itself)
- scope: the uncommitted working-tree diff to install.mjs, tests/installer.test.mjs, AGENTS.md — the uninstaller ownership-proof hardening produced by feeding back review b408a5e3's blocking findings across three GPT-5.4 verification rounds
- verdict: pass — 0 blocking, 4 advisory
- recorded: 2026-07-21

## Review chain context

Review b408a5e3 (second_model, codex-gpt-5.4) found 2 blocking defects in the
uninstaller. Two further Codex verification rounds on the successive fixes
found, cumulatively: bin/lib-only hashing letting stowaway files ride into
deletion; the empty-set hash collision (`e3b0c44298fc`); discriminator-only
control-file checks; empty directories invisible to the hash; control shapes
shallower than the writers; and a swallowed `sourceSkillNames` failure
reopening silent preservation. All were fixed; a fourth Codex confirmation
round wedged twice (a stuck collaboration-wait, then a process that exited
without a result), so the zero-blocking confirmation was conducted by a
different second model (opus-4-8) at the owner's direction.

## Blocking (spec axis)

None. The reviewer specifically re-attacked: symlinked entries (treeManifest
throws → unprovable), mixed provable/unprovable runtime roots, interrupted
installs' journals, dry-run parity, and the install-path refactor
(byte-identical extraction, verified line-by-line).

## Advisory (standards axis)

1. `CONTROL_FILE_SHAPES` journal status list omitted
   `needs_manual_intervention`, which the real writer persists when an asset
   error blocks a release (install.mjs:1021): a genuine journal of a blocked
   release stayed preserved forever instead of deletable. Safe direction
   (over-preservation), but incomplete. **Applied after the review as the
   reviewer's own one-word prescription**; installer suite re-run green
   (16/16).
2. The `sourceSkillNames` failure warning (skills/ unreadable) has no
   dedicated test.
3. `spawnSync("npm", ..., { shell: win32 })` in the tarball test concatenates
   args unescaped (DEP0190): breaks on a profile path containing a space.
   Noted, not fixed — spawning `npm.cmd` directly is EINVAL on Node ≥22, so
   any fix needs explicit quoting instead.
4. `.workloop-active-release.json` shape pins required fields and domains but
   not the absence of extra keys, unlike the sibling shapes. Consistency
   note only.

## Notes

- Suite runs by the reviewer: first combined run 15/16 with one
  `EPERM: rename` transient in the tarball test (Windows file-lock/AV
  contention in `copyManagedTree`'s renameSync); the test passes alone and
  the full file passes on rerun (16/16 twice). The same transient appeared
  once in the owner session on the interruption test and likewise passed on
  rerun. Environmental, not a diff defect; if it recurs in CI, look at
  renameSync racing Defender/indexer locks.
- Non-vacuity confirmed for the two most load-bearing fixes: reverting the
  non-empty-file-set requirement flips the `e3b0c44298fc` assertion;
  relaxing any control shape to discriminator-only deletes the
  wrong-shape fixtures and fails their `existsSync` assertions.
- AGENTS.md's updated uninstall-contract sentence matches the code exactly.
- The safety-classifier sidecar was unavailable during this subagent run; the
  owner session verified the reviewer's claims against the code before
  recording (the `needs_manual_intervention` writer path was independently
  confirmed at install.mjs:1021 before applying advisory 1).
