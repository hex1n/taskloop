# Review b408a5e3-92eb-490e-9317-1bce490e513e

- task: a15d791d-7511-43c3-a50f-a457b38a7e1f (install.mjs closes the taskloop-to-workloop upgrade gaps and the open-atomicity defect that locked this repo)
- level: second_model
- reviewer: codex-gpt-5.4 (Codex session 019f82bc-bcdb-71b3-ba02-253e40feee6a)
- scope: commit range 341ac97..c1f5e4a; uncommitted working-tree files excluded
- verdict: fail — 2 blocking, 2 advisory
- recorded: 2026-07-21

## Blocking (spec axis)

1. `install.mjs:843` — the uninstaller deletes the runtime and control files
   without ownership proof. Lines 843–847 recursively `fs.rmSync`
   `.workloop-runtime`, the managed-skills manifest, the active-release
   manifest, and the activation journal on fixed-path existence alone — no
   digest, shape, or active-release check, and no preserve branch. A home that
   never installed workloop but holds same-named foreign content loses data.
   The fresh-home test (`tests/installer.test.mjs:239`) only creates empty
   directories; 221–223 only verify that a pristine install's runtime is
   removed. Fails: "deletes only what the manifest proves it installed …;
   preserves and reports anything unprovable", plus the non-vacuous-regression
   requirement.

2. `install.mjs:816` — modified managed skills are preserved but not reported.
   816–818 record the digest mismatch as `plan("ok", "... preserved")` while
   991–995 deliberately omit all `ok` detail lines from the report, leaving
   only the summary count. Test line 225 only checks the modification
   survives; 229–230 assert the hand-written shim and ledger warnings, never
   the modified-skill report. Fails: "preserves and reports anything
   unprovable".

## Advisory (standards axis)

1. `install.mjs:705` — skill-root alias grouping is implemented twice: inline
   groups in the install path (705–716) and a near-identical
   `skillRootGroups` (771–784) used only by uninstall. A realpath or
   runtime-list rule change must be synchronized in two places. Convention:
   "uninstall shares install.mjs primitives rather than reimplementing them";
   duplicated code.

2. `install.mjs:608` — the shim's generated body and its recognition shape are
   maintained as two independent texts (generators at 609–613, byte-exact
   regexes at 790–793). Changing only the generator makes the uninstaller
   misjudge its own shim as hand-written and preserve it. Convention:
   duplicated code / shotgun surgery (judgment advisory).

## Notes

- The `open` atomicity fix is sound: `lib/task-engine.mjs:626` calls
  `V3_REDUCERS.task_opened(null, event)` directly rather than restating
  reducer checks; tests cover indeterminate and default-policy satisfied
  rejections, assert no ledger pollution, and fail with the call reverted.
- The entry guard compares realpaths on both sides; the packaging test
  unpacks under `os.tmpdir()` and executes the installer, so reverting the
  fix on macOS (`/var` → `/private/var`) skips `main()` and downstream
  shim/info assertions fail. CI runs `npm test` on macOS.
- Packaging tests check required/excluded content both ways, `UNLICENSED`,
  and tarball install/uninstall; reverting the files allowlist or the license
  change fails. `engines >=22` is not directly asserted but matches CI's
  Node 22/24; not an independent coverage gap.
- The uninstall regression tests are non-vacuous overall (reverting the
  uninstaller fails them) but do not cover the ownership and reporting
  boundaries above.
- The reviewer could not run `npm test` to completion inside its sandbox
  (`spawn EPERM` when the test runner forks); suite greenness was not
  independently confirmed by the reviewer. It is confirmed by this
  repository's own criterion runs.
- The already-accepted unsatisfied-witness proof gap was not raised as a new
  finding.
